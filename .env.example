# Database Configuration
# PostgreSQL connection string
# Format: postgres://username:password@host:port/database?sslmode=disable
DB_URL="postgres://your_username:your_password@localhost:5432/chirpy?sslmode=disable"

# Application Environment
# Set to "dev" for development mode (enables admin reset endpoint)
# Set to "prod" for production mode
PLATFORM="dev"

# JWT Authentication Secret
# Generate a secure random string for signing JWT tokens
# Example: use `openssl rand -base64 64` to generate a secret
JWT_SECRET="your-secret-jwt-key-here-replace-with-random-string"

# Polka Payment Webhook API Key
# API key for authenticating Polka payment webhook requests
POLKA_KEY="your-polka-api-key-here"
